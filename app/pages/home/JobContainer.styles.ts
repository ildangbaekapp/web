import styled from "styled-components";

export const JobContainer = styled.div`
  display: flex;
  overflow-x: auto;
  box-sizing: border-box;
  padding: 10px;
  width: 100%;
  gap: max(10px, 2vw);
`;
